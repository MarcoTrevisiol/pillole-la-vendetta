# Double Counting: esempi 3

Carrellata di esempi con funzioni di teoria dei numeri:

- numero di divisori
- valutazione `p`-adica di `n!`
- definizione di `Phi` di Eulero e formula: `n = sum_d Phi(d)`

